t# Phishsense Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/your-project/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Service Role Key (for admin operations)
# ⚠️  WARNING: Never commit this to version control!
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Development Settings
NODE_ENV=development

# Security Notes:
# 1. Never commit .env.local to version control (it's in .gitignore)
# 2. Add .env.local to your .gitignore if not already there
# 3. Use different values for development and production
# 4. Rotate keys regularly and after any security incident
# 5. Consider using environment variable encryption for production
# 6. Never share or expose these credentials publicly
